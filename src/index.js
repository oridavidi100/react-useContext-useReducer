import React from 'react';
import ReactDOM from 'react-dom';
import MainContainer from './template/core/MainContainer';

ReactDOM.render(
  <React.StrictMode>
    <MainContainer />
  </React.StrictMode>,
  document.getElementById('root')
);
